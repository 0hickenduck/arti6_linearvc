# PRD: Repo Migration and Cleanup

## Objective
Migrate legacy files and directories to the new Trellis-compliant repository structure to ensure a single source of truth and a clean workspace.

## Requirements
1. Archive `research_notes/` into `archive/research_notes/`.
2. Archive `research_harness/` into `archive/provisional_research_harness/`.
3. Delete obsolete state files: `PROJECT_STATE.json` and `AGENTS.md`.
4. Create the main project directory `arti6_linearvc_demo/`.